cargo-features = ["named-profiles"]

[workspace]
members = [
    "jsonrpc-client",
    "solar", 
    "program", 
    "program-so", 
    "disassembler"
]

[profile.bpf-release]
inherits = "release"
lto = true
codegen-units = 1
opt-level = 3
overflow-checks = false
# debug = true
